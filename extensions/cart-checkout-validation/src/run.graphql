query RunInput {
  cart {
    lines {
      quantity
      sellingPlanAllocation {
        sellingPlan {
          id
          name
        }
      }
    }
  }
  shop {
    metafield(namespace: "$app:tryonify", key: "sellingPlans") {
      value
    }
  }
  validation {
    metafield(namespace: "$app:settings", key: "maxTrialItems") {
      value
    }
  }
}
